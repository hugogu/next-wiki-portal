Wiki AI 可以使用**内置的、MCP 兼容的 `next-wiki` 工具提供方**，在与 Web UI 和 REST API 相同的权限受检服务上执行操作。这正是 next-wiki 成为 Agent Runtime 而非聊天框的原因：AI 不只是谈论你的 wiki —— 它能在可见的边界内真正动手建设它。

> **Skill：** 运行时按 MCP & Skill 风格的可扩展性设计。目前 Wiki AI 内仅启用内置 wiki 提供方；外部 MCP 提供方在此阶段**有意不做**自动发现或激活。外部 AI 客户端直连请使用独立打包的 [MCP server](/docs/mcp-server)。

## 运行时提供什么

- **读取工具** —— 搜索、页面获取、页面列表、标签、反向链接与页面邻居。
- **起草与组织工具** —— 创建/保存页面草稿、修改元数据与属性、管理标签、提议批量操作。
- **有界的、提供商无关的工具循环**，聊天中实时显示工具调用状态。
- **服务端强制执行的策略** —— 风险、权限、保留与审查规则在服务端执行，而不是"建议"给模型。
- **管理员提案审查** —— 批量变更落地前的批准、拒绝、应用、冲突检测与逐条结果。
- **工具证据 raw 条目** —— 当工具输出应成为耐久素材时，会被捕获为证据。
- **审计事件与常规发布边界** —— 耐久变更仍然走标准的修订/发布路径。

## 为什么治理重要

- 工具执行**复用权限受检的服务**，并在提案被审查或应用时重新检查权限。
- AI 动作与长耗时工作使用基于 PostgreSQL 的**异步任务生命周期**，不会阻塞请求处理器。
- 每个耐久变更都有版本 —— 覆盖人类编辑的不可变修订保证，同样覆盖 agent 编辑。

## 外部客户端

Claude Code、Cursor、OpenCode、OpenClaw 等 MCP 兼容客户端通过独立打包的 [`@next-wiki/mcp-server`](/docs/mcp-server) 接入同一套 API —— 检索、创作、发布、历史、图谱导航、raw 证据与批量操作。
